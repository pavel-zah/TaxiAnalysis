version: "3.9"
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: "taxi"
    volumes:
      - ./db:/docker-entrypoint-initdb.d
      - ./pg_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    env_file:
      - .env
